# Pattern Project - Dependencies

# Core
python-dotenv>=1.0.0

# LLM Clients
anthropic>=0.78.0
requests>=2.31.0

# Embeddings (lazy loaded - sentence-transformers pulls compatible torch)
sentence-transformers>=2.2.0

# Database
# sqlite3 is built-in

# CLI Interface
rich>=13.0.0

# HTTP API (legacy Flask API, still used when HTTP_ENABLED=true)
flask>=3.0.0

# Web UI (FastAPI + WebSocket, default interface)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Utilities
numpy>=1.24.0

# Text-to-Speech (OpenAI TTS)
openai>=1.0.0

# Speech-to-Text (faster-whisper, lazy-loaded)
faster-whisper>=0.9.0

# Telegram Bot API
python-telegram-bot>=21.0

# Visual Capture (optional)
# Uncomment if using visual features
# pillow>=10.0.0
# opencv-python>=4.8.0
# google-generativeai>=0.3.0

# Clipboard support
pyperclip>=1.8.0

# Reddit API
praw>=7.7.0

# Browser Automation (delegate sub-agent)
# After installing, run: playwright install chromium
playwright>=1.40.0

# TOML parsing (Python < 3.11 fallback; 3.11+ has tomllib built-in)
tomli>=2.0.0

# Google Calendar API
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.0
