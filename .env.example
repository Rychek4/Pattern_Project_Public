# Pattern Project - Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# LLM API KEYS
# =============================================================================
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-opus-4-6

# =============================================================================
# KOBOLDCPP (LOCAL LLM)
# =============================================================================
# Start KoboldCpp with:
# koboldcpp.exe --model Meta-Llama-3.1-8B-Instruct-Q6_K.gguf --host 127.0.0.1 --port 5001 --gpulayers 99 --contextsize 4096
KOBOLD_API_URL=http://127.0.0.1:5001
KOBOLD_MAX_CONTEXT=4096
KOBOLD_MAX_LENGTH=512

# =============================================================================
# LLM ROUTING
# =============================================================================
# Primary provider for conversation: 'anthropic' or 'kobold'
LLM_PRIMARY_PROVIDER=anthropic

# =============================================================================
# HTTP API
# =============================================================================
HTTP_HOST=127.0.0.1
HTTP_PORT=5000

# =============================================================================
# LOGGING
# =============================================================================
LOG_LEVEL=INFO

# =============================================================================
# FEATURE TOGGLES
# =============================================================================
# Master switches for all tool features. Each adds tools to the AI's prompt.
# Disabling unused features reduces prompt token count.
# Values: true / false (unless noted otherwise)

# Intention system — reminders, goals, plans (4 tools)
INTENTION_ENABLED=true

# Telegram messaging (1 tool) — also requires credentials below
TELEGRAM_ENABLED=true

# Email gateway (1 tool) — also requires credentials below
EMAIL_GATEWAY_ENABLED=false

# System pulse — idle timer self-prompting (1 tool)
SYSTEM_PULSE_ENABLED=true

# Curiosity engine — topic exploration during conversation (1 tool)
CURIOSITY_ENABLED=true

# Clipboard — read/write system clipboard (2 tools)
# Requires: pip install pyperclip (and xclip on Linux)
CLIPBOARD_ENABLED=true

# Clarification — AI can pause and ask user for input (1 tool)
CLARIFICATION_ENABLED=true

# Web search — Claude's built-in web search (1 built-in tool)
WEB_SEARCH_ENABLED=true

# Web fetch — Claude's built-in page fetcher (1 built-in tool + 2 domain mgmt tools)
WEB_FETCH_ENABLED=true

# Visual capture — screenshot and webcam (up to 2 tools depending on mode)
# Values: true / false
VISUAL_ENABLED=true
# Per-source capture modes. Values: "auto", "on_demand", "disabled"
#   auto      — captures automatically every prompt, NO tool added
#   on_demand — AI requests capture via tool, tool IS added to prompt
#   disabled  — source never used, no tool added
VISUAL_SCREENSHOT_MODE=auto
VISUAL_WEBCAM_MODE=on_demand

# Moltbook — AI social network (8 tools) — also requires credentials below
MOLTBOOK_ENABLED=false

# Reddit integration (8 tools) — also requires credentials below
REDDIT_ENABLED=false

# =============================================================================
# TELEGRAM CREDENTIALS
# =============================================================================
# Create a bot via @BotFather on Telegram to get your token.
telegram_bot=
TELEGRAM_CHAT_ID=

# =============================================================================
# EMAIL CREDENTIALS
# =============================================================================
# Requires a Gmail app password (not regular password).
# See: https://myaccount.google.com/apppasswords
APP_EMAIL_ADDRESS=
APP_EMAIL_PASS=

# =============================================================================
# VISUAL CAPTURE CREDENTIALS
# =============================================================================
VISUAL_CAPTURE_INTERVAL=30
# Google API key for Gemini 2.5 Flash (image interpretation)
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# MOLTBOOK CREDENTIALS
# =============================================================================
# Run: python scripts/moltbook_register.py --agent-name "YourAgent" --x-handle "@yourhandle"
# Then paste the resulting API key here.
MOLTBOOK_API_KEY=
MOLTBOOK_USER_AGENT=Molt/1.0 (OpenClaw; Pattern-Agent)

# =============================================================================
# REDDIT CREDENTIALS
# =============================================================================
# See docs/reddit_setup.md for full setup instructions.
# 1. Create a "script" app at https://www.reddit.com/prefs/apps
# 2. Fill in the values below
# 3. Run: python scripts/reddit_setup.py to validate
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_PASSWORD=
REDDIT_USER_AGENT=python:pattern-agent:v1.0 (by /u/your_username)
